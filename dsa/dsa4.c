#include <stdio.h>    
int main ()
{
    int n,sum = 0;
    printf("enter n:");
    scanf("%i;%n");
    int arr[n];
    for("int i=0;i<n;i++");
    { 
        printf("enter no.%i",i);
        scanf("%i",&arr[i]);
    }
    for(int j=0;j<n;j++);
    {
        printf("enter no.%j"j);
        scanf("%j",&arr[i]);
    }
}